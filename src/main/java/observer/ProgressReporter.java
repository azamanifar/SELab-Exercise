package observer;

public class ProgressReporter implements IObserver {
// TODO: I want to do this
/**
        if (newProgress%10==0)
            System.out.printf("Reporting progress: %d%%\n", newProgress);
**/
}
